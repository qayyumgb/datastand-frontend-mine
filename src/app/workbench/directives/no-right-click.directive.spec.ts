import { NoRightClickDirective } from './no-right-click.directive';

describe('NoRightClickDirective', () => {
  it('test initialization an instance', () => {
    const directive = new NoRightClickDirective();
    expect(directive).toBeTruthy();
  });
});
